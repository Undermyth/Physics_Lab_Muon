这个文件夹里记录的是探索峰右侧曲线的形状规律的记录，以及相应的参数结果和可视化图片。
经过一堆计算，发现在$U=-e^{at+b}$中，a的值基本是恒定的，与峰的高度无关。
a_result.txt          储存了最终计算平均得到的a值。
calc_a.py             计算a的平均值，没啥用
curve.csv             一行一个数，1000行，记录每个样本在峰右侧得到的a值。
minval.csv            一行一个数，1000行，记录每个样本的真实峰值。
min-a-relation.xlsx   把上两个文件拿到excel里，去掉一些异常值后得到的数据。
validation.py         验证得到的a参数的正确性。
