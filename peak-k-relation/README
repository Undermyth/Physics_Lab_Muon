这个文件夹记录了探索峰左侧下降沿斜率和峰值的关系的过程。包含最终得到的参数和可视化结果。
结果发现，下降沿的斜率与峰值成正比，即下降沿经历的时间是恒定的。
tangent.py                计算峰左侧下降沿的斜率，并在1000份数据上进行测试。
ki.csv                    1000行，每行是每个样本的下降沿斜率。
minvalue.csv              1000行，每行是每个样本的峰值。
minvalue-k-relation.xlsx  把上两个文件合并到excel，手动剔除异常数据
